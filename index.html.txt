<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Neurosight - Alzheimer's Disease Early Detection Platform</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800;900&family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #4A6BFF;
            --secondary: #00D4AA;
            --accent: #FF4D94;
            --accent2: #FFB74D;
            --accent3: #9C4DFF;
            --dark: #1A1A2E;
            --light: #F0F5FF;
            --white: #FFFFFF;
            --gray: #8A8AA3;
            --gradient-primary: linear-gradient(135deg, var(--primary) 0%, var(--accent3) 100%);
            --gradient-secondary: linear-gradient(135deg, var(--secondary) 0%, #00A8CC 100%);
            --gradient-accent: linear-gradient(135deg, var(--accent) 0%, #FF6B9D 100%);
            --gradient-dark: linear-gradient(135deg, var(--dark) 0%, #16213E 100%);
            --shadow-lg: 0 20px 60px rgba(0, 0, 0, 0.15);
            --shadow-xl: 0 30px 80px rgba(0, 0, 0, 0.2);
            --shadow-primary: 0 10px 30px rgba(74, 107, 255, 0.3);
            --transition: all 0.5s cubic-bezier(0.34, 1.56, 0.64, 1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', 'Montserrat', sans-serif;
            line-height: 1.7;
            color: var(--dark);
            background-color: var(--light);
            overflow-x: hidden;
            scroll-behavior: smooth;
        }

        .container {
            width: 100%;
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 40px;
        }

        /* Particle Background */
        #particles-js {
            position: fixed;
            width: 100%;
            height: 100%;
            z-index: -1;
        }

        /* Header Navigation */
        header {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            box-shadow: 0 5px 30px rgba(0, 0, 0, 0.05);
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            animation: slideDown 0.8s ease-out;
        }

        @keyframes slideDown {
            from { transform: translateY(-100%); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }

        .header-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 0;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 15px;
            text-decoration: none;
            transition: var(--transition);
        }

        .logo:hover {
            transform: scale(1.05);
        }

        .logo-icon {
            width: 55px;
            height: 55px;
            background: var(--gradient-primary);
            border-radius: 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--white);
            font-size: 26px;
            font-weight: 800;
            box-shadow: var(--shadow-primary);
            position: relative;
            overflow: hidden;
        }

        .logo-icon:after {
            content: '';
            position: absolute;
            width: 200%;
            height: 200%;
            background: linear-gradient(rgba(255,255,255,0.3) 0%, transparent 50%);
            transform: rotate(45deg);
            top: -50%;
            left: -50%;
            animation: shine 4s infinite;
        }

        @keyframes shine {
            0% { transform: translateX(-100%) translateY(-100%) rotate(45deg); }
            100% { transform: translateX(100%) translateY(100%) rotate(45deg); }
        }

        .logo-text {
            display: flex;
            flex-direction: column;
        }

        .logo-main {
            font-size: 32px;
            font-weight: 900;
            background: var(--gradient-primary);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: -0.5px;
        }

        .logo-tagline {
            font-size: 14px;
            color: var(--gray);
            margin-top: -3px;
            font-weight: 600;
        }

        nav ul {
            display: flex;
            list-style: none;
            gap: 40px;
        }

        nav a {
            text-decoration: none;
            color: var(--dark);
            font-weight: 600;
            font-size: 16px;
            transition: var(--transition);
            position: relative;
            padding: 5px 0;
        }

        nav a:after {
            content: '';
            position: absolute;
            width: 0;
            height: 3px;
            background: var(--accent);
            left: 0;
            bottom: 0;
            border-radius: 3px;
            transition: var(--transition);
        }

        nav a:hover {
            color: var(--primary);
        }

        nav a:hover:after {
            width: 100%;
        }

        .cta-button {
            background: var(--gradient-accent);
            color: var(--white);
            border: none;
            padding: 14px 35px;
            border-radius: 50px;
            font-weight: 700;
            font-size: 16px;
            cursor: pointer;
            transition: var(--transition);
            box-shadow: 0 10px 25px rgba(255, 77, 148, 0.3);
            position: relative;
            overflow: hidden;
            letter-spacing: 0.5px;
        }

        .cta-button:before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
            transition: left 0.7s;
        }

        .cta-button:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(255, 77, 148, 0.4);
        }

        .cta-button:hover:before {
            left: 100%;
        }

        .mobile-menu-btn {
            display: none;
            background: none;
            border: none;
            font-size: 28px;
            color: var(--primary);
            cursor: pointer;
            transition: var(--transition);
        }

        /* Hero Section - 重新调整间距 */
        .hero {
            padding: 180px 0 120px;
            position: relative;
            overflow: hidden;
            background: linear-gradient(135deg, #EFF4FF 0%, #F0F5FF 50%, #E6F7FF 100%);
        }

        .hero:before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 20% 30%, rgba(74, 107, 255, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 70%, rgba(0, 212, 170, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 40% 80%, rgba(255, 77, 148, 0.1) 0%, transparent 50%);
        }

        .hero-content {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 80px;
            position: relative;
            z-index: 2;
        }

        .hero-text {
            flex: 1;
            max-width: 650px;
        }

        .hero-badge {
            display: inline-block;
            background: var(--gradient-secondary);
            color: white;
            padding: 8px 20px;
            border-radius: 50px;
            font-size: 14px;
            font-weight: 700;
            margin-bottom: 25px;
            box-shadow: 0 5px 15px rgba(0, 212, 170, 0.3);
            animation: float 3s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }

        .hero-title {
            font-size: 3.8rem;
            font-weight: 900;
            line-height: 1.1;
            margin-bottom: 20px;
            background: linear-gradient(135deg, var(--primary) 0%, var(--accent3) 50%, var(--accent) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .hero-title span {
            display: block;
        }

        .hero-tagline {
            font-size: 1.6rem;
            font-weight: 600;
            color: var(--gray);
            margin-bottom: 35px;
            line-height: 1.5;
        }

        .hero-description {
            font-size: 1.15rem;
            margin-bottom: 45px;
            color: var(--dark);
            line-height: 1.8;
        }

        .hero-buttons {
            display: flex;
            gap: 20px;
            flex-wrap: wrap;
        }

        .secondary-button {
            background-color: transparent;
            color: var(--primary);
            border: 2px solid var(--primary);
            padding: 14px 35px;
            border-radius: 50px;
            font-weight: 700;
            font-size: 16px;
            cursor: pointer;
            transition: var(--transition);
            position: relative;
            overflow: hidden;
        }

        .secondary-button:before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(74, 107, 255, 0.1), transparent);
            transition: left 0.7s;
        }

        .secondary-button:hover {
            background-color: rgba(74, 107, 255, 0.05);
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(74, 107, 255, 0.15);
        }

        .secondary-button:hover:before {
            left: 100%;
        }

        .hero-visual {
            flex: 1;
            max-width: 600px;
            position: relative;
        }

        .brain-visualization {
            width: 100%;
            height: 380px;
            background: var(--gradient-dark);
            border-radius: 30px;
            position: relative;
            overflow: hidden;
            box-shadow: var(--shadow-xl);
            transform-style: preserve-3d;
            perspective: 1000px;
        }

        .brain-visualization:before {
            content: '';
            position: absolute;
            width: 200%;
            height: 200%;
            background: 
                radial-gradient(circle at 30% 30%, rgba(74, 107, 255, 0.3) 0%, transparent 50%),
                radial-gradient(circle at 70% 70%, rgba(0, 212, 170, 0.3) 0%, transparent 50%),
                radial-gradient(circle at 50% 50%, rgba(255, 77, 148, 0.3) 0%, transparent 50%);
            animation: rotate 30s linear infinite;
        }

        .eye-tracker-demo {
            position: absolute;
            width: 110px;
            height: 110px;
            background: var(--white);
            border-radius: 50%;
            top: 70px;
            left: 70px;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.2);
            animation: pulse 2s ease-in-out infinite;
            z-index: 3;
        }

        .eye-tracker-demo:before {
            content: '';
            width: 45px;
            height: 45px;
            background: var(--accent);
            border-radius: 50%;
            position: absolute;
            animation: eyeMove 4s infinite alternate;
        }

        @keyframes eyeMove {
            0%, 100% { transform: translate(-15px, -10px); }
            25% { transform: translate(15px, -10px); }
            50% { transform: translate(15px, 10px); }
            75% { transform: translate(-15px, 10px); }
        }

        .mri-visualization {
            position: absolute;
            width: 230px;
            height: 230px;
            border-radius: 50%;
            border: 8px solid rgba(255, 255, 255, 0.5);
            top: 50px;
            right: 70px;
            animation: pulse 3s infinite;
            z-index: 2;
        }

        .mri-visualization:before {
            content: '';
            position: absolute;
            width: 100%;
            height: 100%;
            border-radius: 50%;
            border: 8px solid rgba(255, 255, 255, 0.3);
            top: -16px;
            left: -16px;
            animation: pulse 3s infinite 0.5s;
        }

        .data-flow {
            position: absolute;
            bottom: 50px;
            left: 50%;
            transform: translateX(-50%);
            width: 80%;
            height: 90px;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            display: flex;
            justify-content: space-around;
            align-items: center;
            padding: 0 30px;
            z-index: 4;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        }

        .data-point {
            text-align: center;
            position: relative;
        }

        .data-value {
            font-size: 2.2rem;
            font-weight: 900;
            color: var(--white);
            margin-bottom: 5px;
            text-shadow: 0 2px 10px rgba(0, 0, 0, 0.2);
        }

        .data-label {
            font-size: 0.85rem;
            color: rgba(255, 255, 255, 0.9);
            font-weight: 600;
        }

        /* 统一调整内容区块的间距 */
        .section {
            padding: 120px 0;
            position: relative;
        }

        .section-title {
            text-align: center;
            font-size: 3.2rem;
            font-weight: 900;
            margin-bottom: 20px;
            position: relative;
        }

        .section-title span {
            background: var(--gradient-primary);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            position: relative;
            display: inline-block;
        }

        .section-title span:after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 0;
            width: 100%;
            height: 5px;
            background: var(--gradient-primary);
            border-radius: 3px;
            transform: scaleX(0.3);
            transition: transform 1s ease;
        }

        .section.in-view .section-title span:after {
            transform: scaleX(1);
        }

        .section-subtitle {
            text-align: center;
            font-size: 1.25rem;
            color: var(--gray);
            max-width: 800px;
            margin: 0 auto 70px;
            line-height: 1.8;
        }

        /* 问题与解决方案 - 调整间距 */
        .problem-solution-container {
            display: flex;
            gap: 50px;
            margin-bottom: 80px;
        }

        .problem-box, .solution-box {
            flex: 1;
            background: var(--white);
            border-radius: 25px;
            padding: 45px;
            box-shadow: var(--shadow-lg);
            transition: var(--transition);
            position: relative;
            overflow: hidden;
        }

        .problem-box:before, .solution-box:before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 8px;
        }

        .problem-box:before {
            background: var(--gradient-accent);
        }

        .solution-box:before {
            background: var(--gradient-secondary);
        }

        .problem-box:hover, .solution-box:hover {
            transform: translateY(-15px);
            box-shadow: var(--shadow-xl);
        }

        .problem-title, .solution-title {
            font-size: 2rem;
            font-weight: 800;
            margin-bottom: 30px;
            display: flex;
            align-items: center;
            gap: 18px;
        }

        .problem-title {
            color: var(--accent);
        }

        .solution-title {
            color: var(--secondary);
        }

        .problem-title i, .solution-title i {
            font-size: 2.5rem;
        }

        .problem-list, .solution-list {
            list-style: none;
        }

        .problem-list li, .solution-list li {
            margin-bottom: 22px;
            padding-left: 35px;
            position: relative;
            line-height: 1.8;
            font-size: 1.05rem;
        }

        .problem-list li:before {
            content: '✗';
            color: var(--accent);
            font-weight: bold;
            position: absolute;
            left: 0;
            font-size: 1.4rem;
        }

        .solution-list li:before {
            content: '✓';
            color: var(--secondary);
            font-weight: bold;
            position: absolute;
            left: 0;
            font-size: 1.4rem;
        }

        /* 技术展示部分 - 调整间距 */
        .tech-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(340px, 1fr));
            gap: 45px;
            margin-top: 70px;
        }

        .tech-card {
            background: var(--white);
            border-radius: 25px;
            padding: 45px 35px;
            box-shadow: var(--shadow-lg);
            transition: var(--transition);
            position: relative;
            overflow: hidden;
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
        }

        .tech-card:after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(74, 107, 255, 0.05), rgba(156, 77, 255, 0.05));
            z-index: 0;
        }

        .tech-card:hover {
            transform: translateY(-15px);
            box-shadow: var(--shadow-xl);
        }

        .tech-icon-container {
            width: 110px;
            height: 110px;
            background: var(--gradient-primary);
            border-radius: 25px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 35px;
            color: var(--white);
            font-size: 45px;
            position: relative;
            z-index: 1;
            box-shadow: 0 15px 35px rgba(74, 107, 255, 0.3);
        }

        .tech-title {
            font-size: 1.8rem;
            font-weight: 800;
            margin-bottom: 22px;
            color: var(--dark);
            position: relative;
            z-index: 1;
        }

        /* 关键指标部分 - 调整间距 */
        .metrics-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 45px;
            margin-top: 70px;
        }

        .metric-card {
            background: var(--white);
            border-radius: 25px;
            padding: 45px 35px;
            text-align: center;
            box-shadow: var(--shadow-lg);
            transition: var(--transition);
            position: relative;
            overflow: hidden;
        }

        .metric-card:before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 8px;
            background: var(--gradient-primary);
        }

        .metric-card:hover {
            transform: translateY(-15px);
            box-shadow: var(--shadow-xl);
        }

        .metric-value {
            font-size: 4rem;
            font-weight: 900;
            color: var(--primary);
            margin-bottom: 15px;
            line-height: 1;
            text-shadow: 0 5px 15px rgba(74, 107, 255, 0.2);
        }

        .metric-label {
            font-size: 1.4rem;
            color: var(--dark);
            font-weight: 800;
            margin-bottom: 22px;
        }

        .metric-desc {
            font-size: 1.05rem;
            color: var(--gray);
            line-height: 1.8;
        }

        /* 团队部分 - 调整间距 */
        .team-container {
            display: flex;
            justify-content: center;
            gap: 50px;
            margin-top: 70px;
            flex-wrap: wrap;
        }

        .team-card {
            width: 360px;
            background: var(--white);
            border-radius: 25px;
            overflow: hidden;
            box-shadow: var(--shadow-lg);
            transition: var(--transition);
            position: relative;
        }

        .team-card:hover {
            transform: translateY(-15px);
            box-shadow: var(--shadow-xl);
        }

        .team-card:nth-child(1) .team-avatar {
            background: var(--gradient-primary);
        }

        .team-card:nth-child(2) .team-avatar {
            background: var(--gradient-accent);
        }

        .team-avatar {
            height: 260px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--white);
            font-size: 5.5rem;
            position: relative;
            overflow: hidden;
        }

        .team-avatar:after {
            content: '';
            position: absolute;
            width: 200%;
            height: 200%;
            background: rgba(255, 255, 255, 0.1);
            transform: rotate(45deg);
            top: -50%;
            left: -50%;
            animation: shimmer 3s infinite;
        }

        .team-info {
            padding: 35px;
        }

        .team-name {
            font-size: 1.9rem;
            font-weight: 900;
            margin-bottom: 10px;
            color: var(--dark);
        }

        .team-role {
            font-size: 1.2rem;
            color: var(--accent);
            margin-bottom: 22px;
            font-weight: 700;
        }

        .team-desc {
            font-size: 1.05rem;
            color: var(--gray);
            line-height: 1.8;
        }

        /* 团队引言部分 */
        .team-quote {
            text-align: center;
            margin-top: 70px;
        }

        .team-quote p {
            font-style: italic;
            color: var(--gray);
            max-width: 800px;
            margin: 0 auto;
            font-size: 1.15rem;
            line-height: 1.8;
            background: rgba(255,255,255,0.7);
            padding: 30px;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.05);
        }

        /* Footer - 调整间距 */
        footer {
            background: var(--gradient-dark);
            color: var(--white);
            padding: 90px 0 40px;
            margin-top: 120px;
            position: relative;
            overflow: hidden;
        }

        footer:before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 20% 30%, rgba(74, 107, 255, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 70%, rgba(0, 212, 170, 0.1) 0%, transparent 50%);
        }

        .footer-container {
            display: flex;
            flex-wrap: wrap;
            gap: 60px;
            margin-bottom: 50px;
            position: relative;
            z-index: 1;
        }

        .footer-column {
            flex: 1;
            min-width: 250px;
        }

        .footer-title {
            font-size: 1.7rem;
            font-weight: 900;
            margin-bottom: 30px;
            color: var(--white);
            position: relative;
            display: inline-block;
        }

        .footer-title:after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 0;
            width: 50px;
            height: 4px;
            background: var(--accent2);
            border-radius: 2px;
        }

        .footer-links {
            list-style: none;
        }

        .footer-links li {
            margin-bottom: 18px;
        }

        .footer-links a {
            color: rgba(255, 255, 255, 0.8);
            text-decoration: none;
            transition: var(--transition);
            display: inline-block;
            font-size: 1.05rem;
        }

        .footer-links a:hover {
            color: var(--white);
            transform: translateX(8px);
        }

        .contact-info {
            display: flex;
            flex-direction: column;
            gap: 22px;
        }

        .contact-item {
            display: flex;
            align-items: center;
            gap: 18px;
            color: rgba(255, 255, 255, 0.9);
            font-size: 1.05rem;
        }

        .contact-item i {
            color: var(--accent2);
            font-size: 1.3rem;
        }

        .copyright {
            text-align: center;
            padding-top: 40px;
            border-top: 1px solid rgba(255, 255, 255, 0.2);
            color: rgba(255, 255, 255, 0.7);
            font-size: 0.95rem;
            position: relative;
            z-index: 1;
        }

        /* 动画类 */
        .fade-in {
            opacity: 0;
            transform: translateY(50px);
            transition: opacity 1s ease, transform 1s ease;
        }

        .fade-in.visible {
            opacity: 1;
            transform: translateY(0);
        }

        /* 响应式设计 */
        @media (max-width: 1200px) {
            .hero-content {
                flex-direction: column;
                text-align: center;
                gap: 80px;
            }
            
            .hero-title {
                font-size: 3.2rem;
            }
            
            .problem-solution-container {
                flex-direction: column;
            }
        }

        @media (max-width: 992px) {
            .section-title {
                font-size: 2.8rem;
            }
            
            .section-subtitle {
                font-size: 1.2rem;
                margin-bottom: 60px;
            }
            
            .hero-title {
                font-size: 2.8rem;
            }
            
            .hero-tagline {
                font-size: 1.4rem;
            }
            
            .tech-container, .metrics-container {
                grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            }
        }

        @media (max-width: 768px) {
            .container {
                padding: 0 25px;
            }
            
            nav ul {
                display: none;
                position: absolute;
                top: 100%;
                left: 0;
                width: 100%;
                background: var(--white);
                flex-direction: column;
                padding: 25px;
                box-shadow: 0 15px 40px rgba(0,0,0,0.1);
                z-index: 100;
                gap: 20px;
            }
            
            nav ul.active {
                display: flex;
            }
            
            .mobile-menu-btn {
                display: block;
            }
            
            .hero {
                padding: 150px 0 80px;
            }
            
            .hero-title {
                font-size: 2.5rem;
            }
            
            .hero-tagline {
                font-size: 1.3rem;
            }
            
            .hero-description {
                font-size: 1.1rem;
            }
            
            .section {
                padding: 90px 0;
            }
            
            .section-title {
                font-size: 2.4rem;
            }
            
            .section-subtitle {
                font-size: 1.1rem;
                margin-bottom: 50px;
            }
            
            .problem-box, .solution-box {
                padding: 35px 25px;
            }
            
            .problem-title, .solution-title {
                font-size: 1.8rem;
            }
            
            .team-container {
                gap: 35px;
            }
            
            .data-flow {
                flex-wrap: wrap;
                height: auto;
                padding: 20px;
                gap: 20px;
            }
            
            .tech-container, .metrics-container {
                grid-template-columns: 1fr;
                gap: 35px;
            }
            
            .footer-container {
                gap: 40px;
            }
            
            .footer-title {
                font-size: 1.5rem;
            }
        }

        @media (max-width: 480px) {
            .hero-title {
                font-size: 2.2rem;
            }
            
            .hero-buttons {
                flex-direction: column;
                width: 100%;
            }
            
            .cta-button, .secondary-button {
                width: 100%;
                text-align: center;
            }
            
            .section-title {
                font-size: 2rem;
            }
            
            .team-card {
                width: 100%;
            }
            
            .metric-value {
                font-size: 3.5rem;
            }
        }
    </style>
</head>
<body>
    <!-- Particle Background -->
    <div id="particles-js"></div>

    <!-- Header Navigation -->
    <header>
        <div class="container header-container">
            <a href="#" class="logo">
                <div class="logo-icon">N</div>
                <div class="logo-text">
                    <div class="logo-main">Neurosight</div>
                    <div class="logo-tagline">Intelligent Cognitive Health Guardian</div>
                </div>
            </a>
            
            <button class="mobile-menu-btn" id="mobileMenuBtn">
                <i class="fas fa-bars"></i>
            </button>
            
            <nav>
                <ul id="mainNav">
                    <li><a href="#home">Home</a></li>
                    <li><a href="#problem">Problem & Solution</a></li>
                    <li><a href="#technology">Core Technology</a></li>
                    <li><a href="#metrics">Key Metrics</a></li>
                    <li><a href="#team">Our Team</a></li>
                    <li><a href="#contact">Contact Us</a></li>
                </ul>
            </nav>
            
            <button class="cta-button" id="demoBtn">Schedule Demo</button>
        </div>
    </header>

    <!-- Hero Section -->
    <section class="hero" id="home">
        <div class="container hero-content">
            <div class="hero-text">
                <div class="hero-badge">Revolutionary AI Detection Platform</div>
                <h1 class="hero-title">
                    <span>Early Detection, Early Protection</span>
                    <span>Neurosight Redefines</span>
                    <span>Alzheimer's Disease Screening</span>
                </h1>
                <p class="hero-tagline">Combining AI eye-tracking with MRI intelligent analysis for 10-minute pre-screening with >90% accuracy, identifying risks 2-3 years earlier</p>
                <p class="hero-description">
                    Neurosight is a breakthrough AI platform that redefines the early detection pathway for Alzheimer's disease. By combining patient-friendly digital eye-movement screening with clinically-validated MRI analysis AI engine, we achieve 10-minute pre-screening with over 90% accuracy, enabling risk identification 2-3 years earlier and improving diagnostic efficiency by 70%.
                </p>
                <div class="hero-buttons">
                    <button class="cta-button" id="learnMoreBtn">Learn More</button>
                    <button class="secondary-button" id="tryDemoBtn">Try Demo</button>
                </div>
            </div>
            
            <div class="hero-visual">
                <div class="brain-visualization">
                    <div class="eye-tracker-demo"></div>
                    <div class="mri-visualization"></div>
                    <div class="data-flow">
                        <div class="data-point">
                            <div class="data-value">>90%</div>
                            <div class="data-label">Screening Accuracy</div>
                        </div>
                        <div class="data-point">
                            <div class="data-value">10 min</div>
                            <div class="data-label">Quick Screening</div>
                        </div>
                        <div class="data-point">
                            <div class="data-value">70%↑</div>
                            <div class="data-label">Efficiency Boost</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Problem & Solution -->
    <section class="section" id="problem">
        <div class="container">
            <h2 class="section-title"><span>Problem & Solution</span></h2>
            <p class="section-subtitle">Alzheimer's disease is a major healthcare challenge in our aging society, with a new case emerging globally every 3 seconds. Early symptoms are often overlooked, leading to irreversible brain damage before diagnosis.</p>
            
            <div class="problem-solution-container">
                <div class="problem-box fade-in">
                    <h3 class="problem-title"><i class="fas fa-exclamation-triangle"></i>Current Challenges</h3>
                    <ul class="problem-list">
                        <li>Cognitive and Healthcare Barriers: Early symptoms are mistaken for normal aging. Primary care lacks effective, objective screening tools</li>
                        <li>Inefficient Resource Allocation: Referring all suspected patients for MRI is financially and logistically unsustainable, causing long wait times and high costs</li>
                        <li>Irreversible Consequences: By the time definitive diagnosis is made, the "golden window" for early intervention has often closed, leading to significantly higher long-term care costs and personal burden</li>
                        <li>Over 17 million patients in China alone. Global diagnosis and treatment market valued at $7-8 billion in 2019, projected to reach $15-20 billion by 2027-2030</li>
                    </ul>
                </div>
                
                <div class="solution-box fade-in">
                    <h3 class="solution-title"><i class="fas fa-lightbulb"></i>Neurosight Solution</h3>
                    <ul class="solution-list">
                        <li>Innovative Two-Stage Fusion Architecture: Seamless integration of non-invasive eye-movement screening with medical imaging intelligent auxiliary diagnosis</li>
                        <li>10-Minute Pre-screening: Real-time eye movement capture via standard cameras, analysis of four standardized cognitive tasks to identify early cognitive impairment signs</li>
                        <li>AI MRI Analysis: Vision Transformer-based deep learning model for automatic brain image analysis, outputting four-stage graded diagnosis</li>
                        <li>End-to-End Tiered Diagnosis: From home screening to hospital precision diagnosis, enabling simultaneous lesion detection and severity grading</li>
                        <li>Sustainable Technology Barriers: Core algorithms eligible for patent protection, high-quality datasets and fusion strategies maintained as trade secrets</li>
                    </ul>
                </div>
            </div>
        </div>
    </section>

    <!-- Core Technology -->
    <section class="section" id="technology">
        <div class="container">
            <h2 class="section-title"><span>Core Technology</span></h2>
            <p class="section-subtitle">Neurosight employs an innovative two-stage fusion architecture, perfectly combining non-invasive early eye-movement screening with medical imaging-based intelligent auxiliary diagnosis</p>
            
            <div class="tech-container">
                <div class="tech-card fade-in">
                    <div class="tech-icon-container">
                        <i class="fas fa-eye"></i>
                    </div>
                    <h3 class="tech-title">Eye-Tracking Screening</h3>
                    <p>Real-time eye movement capture via standard cameras, using Media Pipe for facial landmark extraction, employing 3D ocular geometry modeling, nonlinear coordinate calibration, and hybrid temporal filtering to precisely reconstruct gaze trajectories. Analysis of four standardized cognitive tasks: baseline calibration, image judgment, video tracking, and text reading.</p>
                    <p style="margin-top: 20px; font-weight: 700; color: var(--accent);">Key Innovation: Geometric modeling + nonlinear calibration achieves professional-grade tracking accuracy (±1° error) on consumer devices</p>
                </div>
                
                <div class="tech-card fade-in">
                    <div class="tech-icon-container">
                        <i class="fas fa-brain"></i>
                    </div>
                    <h3 class="tech-title">AI MRI Analysis</h3>
                    <p>Vision Transformer-based deep learning model using multi-head self-attention mechanisms to model whole-brain structural correlations, particularly suitable for detecting Alzheimer's disease characteristic whole-brain atrophy. Combined with Attention Rollout mechanism to generate interpretable heatmaps, visually highlighting key brain regions like hippocampus, temporal cortex, and enlarged ventricles.</p>
                    <p style="margin-top: 20px; font-weight: 700; color: var(--accent);">Performance: Over 95% classification accuracy, AUC performance exceeding 0.99</p>
                </div>
                
                <div class="tech-card fade-in">
                    <div class="tech-icon-container">
                        <i class="fas fa-cogs"></i>
                    </div>
                    <h3 class="tech-title">System Integration</h3>
                    <p>Establishes a complete closed-loop system from daily risk alerts to clinical precision grading. Multi-task cognitive trajectory analysis enhances early screening sensitivity and robustness. System outputs four-stage graded diagnosis (non-dementia, very mild, mild, moderate/severe), providing clinicians with efficient, reliable, and intuitive decision support.</p>
                    <p style="margin-top: 20px; font-weight: 700; color: var(--accent);">Clinical Validation: Collaborated with two regional medical centers, completed preliminary clinical validation studies involving over 500 participants</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Key Metrics -->
    <section class="section" id="metrics">
        <div class="container">
            <h2 class="section-title"><span>Key Metrics</span></h2>
            <p class="section-subtitle">Neurosight has demonstrated exceptional performance in clinical validation, providing a practical solution to the early detection challenge of Alzheimer's disease</p>
            
            <div class="metrics-container">
                <div class="metric-card fade-in">
                    <div class="metric-value">>90%</div>
                    <div class="metric-label">Early Screening Sensitivity</div>
                    <div class="metric-desc">Eye-tracking screening demonstrates over 90% sensitivity in early stages, effectively identifying high-risk individuals</div>
                </div>
                
                <div class="metric-card fade-in">
                    <div class="metric-value">97%</div>
                    <div class="metric-label">MRI Analysis Accuracy</div>
                    <div class="metric-desc">MRI analysis algorithm achieves 97% diagnostic accuracy in identifying Alzheimer's disease-related structural biomarkers</div>
                </div>
                
                <div class="metric-card fade-in">
                    <div class="metric-value">2-3 years</div>
                    <div class="metric-label">Early Warning Time</div>
                    <div class="metric-desc">Based on experimental data, widespread adoption among high-risk elderly populations enables detection 2-3 years earlier</div>
                </div>
                
                <div class="metric-card fade-in">
                    <div class="metric-value">70%↑</div>
                    <div class="metric-label">Diagnostic Efficiency Boost</div>
                    <div class="metric-desc">Integrated approach improves diagnostic efficiency by over 70%, significantly alleviating medical resource pressure</div>
                </div>
                
                <div class="metric-card fade-in">
                    <div class="metric-value">30 cases/1k</div>
                    <div class="metric-label">Moderate-Severe Dementia Prevention</div>
                    <div class="metric-desc">Every 1,000 screenings estimated to prevent approximately 30 cases of moderate-to-severe dementia, generating substantial healthcare cost savings</div>
                </div>
                
                <div class="metric-card fade-in">
                    <div class="metric-value">10 minutes</div>
                    <div class="metric-label">Screening Time</div>
                    <div class="metric-desc">Entire screening process takes only about 10 minutes, enabling non-invasive, low-cost early screening</div>
                </div>
            </div>
        </div>
    </section>

    <!-- Team Section -->
    <section class="section" id="team">
        <div class="container">
            <h2 class="section-title"><span>Our Team</span></h2>
            <p class="section-subtitle">We are a focused professional team with diverse expertise in artificial intelligence, clinical neuroscience, and business strategy, dedicated to transforming technology into practical solutions for Alzheimer's disease early detection</p>
            
            <div class="team-container">
                <div class="team-card fade-in">
                    <div class="team-avatar">
                        <i class="fas fa-code"></i>
                    </div>
                    <div class="team-info">
                        <h3 class="team-name">Gabby</h3>
                        <p class="team-role">Technical Lead & AI Algorithm Expert</p>
                        <p class="team-desc">Responsible for enhancing and developing medical image analysis learning models, ensuring algorithms are based on solid medical science, and leading user experience design for elderly populations. Extensive experience in medical AI research, skilled in deep learning model optimization and healthcare technology deployment.</p>
                    </div>
                </div>
                
                <div class="team-card fade-in">
                    <div class="team-avatar">
                        <i class="fas fa-chart-line"></i>
                    </div>
                    <div class="team-info">
                        <h3 class="team-name">Ashley</h3>
                        <p class="team-role">Business Lead & Market Strategy Expert</p>
                        <p class="team-desc">Primarily responsible for business matters, contributing from economic analysis to marketing strategy planning. Experience in commercializing medical technologies, skilled in market expansion, partnership building, and business model design.</p>
                    </div>
                </div>
            </div>
            
            <div class="team-quote fade-in">
                <p>
                    "What brought us together was witnessing the difficulties of early Alzheimer's diagnosis in our relatives while feeling deeply committed to providing a more accessible solution. As a team, we offer advanced skills in medical AI research, clinical validation, and healthcare technology deployment, along with direct experience from previous research projects with medical institutions."
                </p>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer id="contact">
        <div class="container">
            <div class="footer-container">
                <div class="footer-column">
                    <h3 class="footer-title">Neurosight</h3>
                    <p style="color: rgba(255, 255, 255, 0.9); margin-bottom: 30px; line-height: 1.8; font-size: 1.05rem;">Intelligent Cognitive Health Guardian - Making Alzheimer's Disease Screening Smarter and More Accurate</p>
                    <div class="contact-info">
                        <div class="contact-item">
                            <i class="fas fa-map-marker-alt"></i>
                            <span>Shanghai, China</span>
                        </div>
                        <div class="contact-item">
                            <i class="fas fa-phone"></i>
                            <span>1234578910</span>
                        </div>
                        <div class="contact-item">
                            <i class="fas fa-envelope"></i>
                            <span>qianxinyue1230@163.com</span>
                        </div>
                    </div>
                </div>
                
                <div class="footer-column">
                    <h3 class="footer-title">Quick Links</h3>
                    <ul class="footer-links">
                        <li><a href="#home">Home</a></li>
                        <li><a href="#problem">Problem & Solution</a></li>
                        <li><a href="#technology">Core Technology</a></li>
                        <li><a href="#metrics">Key Metrics</a></li>
                        <li><a href="#team">Our Team</a></li>
                    </ul>
                </div>
                
                <div class="footer-column">
                    <h3 class="footer-title">Target Customers</h3>
                    <ul class="footer-links">
                        <li><a href="#">Individual Users</a></li>
                        <li><a href="#">Hospitals & Diagnostic Centers</a></li>
                        <li><a href="#">Pharmaceutical Companies</a></li>
                        <li><a href="#">Research Institutions</a></li>
                        <li><a href="#">Public Health Departments</a></li>
                    </ul>
                </div>
            </div>
            
            <div class="copyright">
                <p>© 2025 Neurosight. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <!-- Particle Effects Library -->
    <script src="https://cdn.jsdelivr.net/particles.js/2.0.0/particles.min.js"></script>
    <script>
        // Initialize Particle Background
        particlesJS('particles-js', {
            particles: {
                number: { value: 80, density: { enable: true, value_area: 800 } },
                color: { value: ["#4A6BFF", "#00D4AA", "#FF4D94", "#FFB74D", "#9C4DFF"] },
                shape: { type: "circle" },
                opacity: { value: 0.5, random: true },
                size: { value: 3, random: true },
                line_linked: {
                    enable: true,
                    distance: 150,
                    color: "#4A6BFF",
                    opacity: 0.2,
                    width: 1
                },
                move: {
                    enable: true,
                    speed: 2,
                    direction: "none",
                    random: true,
                    straight: false,
                    out_mode: "out",
                    bounce: false
                }
            },
            interactivity: {
                detect_on: "canvas",
                events: {
                    onhover: { enable: true, mode: "repulse" },
                    onclick: { enable: true, mode: "push" }
                }
            },
            retina_detect: true
        });

        // Scroll Animation Effects
        document.addEventListener('DOMContentLoaded', function() {
            const fadeElements = document.querySelectorAll('.fade-in');
            const sections = document.querySelectorAll('.section');
            
            const fadeInOnScroll = function() {
                fadeElements.forEach(element => {
                    const elementTop = element.getBoundingClientRect().top;
                    const elementVisible = 150;
                    
                    if (elementTop < window.innerHeight - elementVisible) {
                        element.classList.add('visible');
                    }
                });
                
                sections.forEach(section => {
                    const sectionTop = section.getBoundingClientRect().top;
                    if (sectionTop < window.innerHeight - 100) {
                        section.classList.add('in-view');
                    }
                });
            };
            
            // Initial Check
            fadeInOnScroll();
            
            // Check on Scroll
            window.addEventListener('scroll', fadeInOnScroll);
            
            // Button Interactions
            document.getElementById('demoBtn').addEventListener('click', function() {
                this.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Submitting...';
                setTimeout(() => {
                    this.innerHTML = '<i class="fas fa-check"></i> Submitted!';
                    setTimeout(() => {
                        this.innerHTML = 'Schedule Demo';
                        alert('Thank you for your interest in Neurosight! Our team will contact you shortly to arrange a demonstration.');
                    }, 500);
                }, 1000);
            });
            
            document.getElementById('learnMoreBtn').addEventListener('click', function() {
                document.getElementById('technology').scrollIntoView({ behavior: 'smooth' });
            });
            
            document.getElementById('tryDemoBtn').addEventListener('click', function() {
                const demoWindow = window.open('', '_blank', 'width=900,height=700,scrollbars=yes');
                demoWindow.document.write(`
                    <html>
                    <head><title>Neurosight Demo Experience</title>
                    <style>
                        body { 
                            font-family: 'Poppins', sans-serif; 
                            padding: 40px; 
                            text-align: center; 
                            background: linear-gradient(135deg, #EFF4FF 0%, #F0F5FF 100%);
                        }
                        .demo-container { 
                            max-width: 700px; 
                            margin: 0 auto; 
                            background: white;
                            border-radius: 30px;
                            padding: 50px;
                            box-shadow: 0 30px 80px rgba(0, 0, 0, 0.15);
                        }
                        .demo-title { 
                            color: #4A6BFF; 
                            font-size: 36px; 
                            font-weight: 900;
                            margin-bottom: 20px;
                            background: linear-gradient(135deg, #4A6BFF 0%, #9C4DFF 100%);
                            -webkit-background-clip: text;
                            -webkit-text-fill-color: transparent;
                        }
                        .demo-text { 
                            margin: 20px 0; 
                            line-height: 1.8;
                            font-size: 18px;
                            color: #555;
                        }
                        .demo-highlight { 
                            background: linear-gradient(135deg, #FF4D94 0%, #FF6B9D 100%);
                            color: white; 
                            padding: 5px 15px; 
                            border-radius: 20px;
                            font-weight: 700;
                        }
                        .demo-task {
                            background: #f8f9ff;
                            border-radius: 20px;
                            padding: 30px;
                            margin: 30px 0;
                            text-align: left;
                        }
                        .demo-task h3 {
                            color: #4A6BFF;
                            margin-bottom: 15px;
                        }
                    </style>
                    </head>
                    <body>
                        <div class="demo-container">
                            <h1 class="demo-title">Neurosight Eye-Tracking Demo</h1>
                            <p class="demo-text">This is a simplified eye-tracking demo. In the actual system, users need to complete four cognitive tasks:</p>
                            
                            <div class="demo-task">
                                <h3>1. Baseline Calibration</h3>
                                <p>The system guides users to focus on several points on the screen to calibrate the eye-tracking system.</p>
                            </div>
                            
                            <div class="demo-task">
                                <h3>2. Image Judgment</h3>
                                <p>Users observe a series of images and answer related questions while the system analyzes eye movement patterns.</p>
                            </div>
                            
                            <div class="demo-task">
                                <h3>3. Video Tracking</h3>
                                <p>Users watch a video and track moving objects while the system analyzes tracking accuracy and latency.</p>
                            </div>
                            
                            <div class="demo-task">
                                <h3>4. Text Reading</h3>
                                <p>Users read a passage of text while the system analyzes reading speed, regression rate, and fixation distribution.</p>
                            </div>
                            
                            <p class="demo-text">The system analyzes eye movement trajectory characteristics such as saccade paths, fixation durations, and regression rates to identify early signs of cognitive impairment.</p>
                            <p class="demo-text">The entire screening process takes only about <span class="demo-highlight">10 minutes</span> with over <span class="demo-highlight">90% accuracy</span>.</p>
                            <p style="margin-top: 40px; font-style: italic; color: #888;">Demo ended - Actual product requires camera connection for real eye-tracking</p>
                        </div>
                    </body>
                    </html>
                `);
            });
            
            // Mobile Menu Toggle
            const mobileMenuBtn = document.getElementById('mobileMenuBtn');
            const mainNav = document.querySelector('nav ul');
            
            mobileMenuBtn.addEventListener('click', function() {
                mainNav.classList.toggle('active');
                this.innerHTML = mainNav.classList.contains('active') ? 
                    '<i class="fas fa-times"></i>' : '<i class="fas fa-bars"></i>';
            });
            
            // Navigation Link Clicks
            document.querySelectorAll('nav a').forEach(link => {
                link.addEventListener('click', function(e) {
                    if (this.getAttribute('href').startsWith('#')) {
                        e.preventDefault();
                        const targetId = this.getAttribute('href');
                        const targetElement = document.querySelector(targetId);
                        
                        if (targetElement) {
                            window.scrollTo({
                                top: targetElement.offsetTop - 80,
                                behavior: 'smooth'
                            });
                            
                            // Close menu on mobile after click
                            if (window.innerWidth <= 768) {
                                mainNav.classList.remove('active');
                                mobileMenuBtn.innerHTML = '<i class="fas fa-bars"></i>';
                            }
                        }
                    }
                });
            });
            
            // Data Point Animation
            const dataValues = document.querySelectorAll('.data-value, .metric-value');
            dataValues.forEach(value => {
                const originalText = value.textContent;
                
                if (originalText.includes('cases') || 
                    originalText.includes('min') || originalText.includes('years')) {
                    return; // Skip non-numeric values
                }
                
                const numericPart = originalText.replace('>', '').replace('%', '').replace('↑', '').replace('/1k', '').replace('cases', '');
                const target = parseFloat(numericPart) || 0;
                
                if (target > 0) {
                    value.textContent = '0';
                    
                    setTimeout(() => {
                        let counter = 0;
                        const increment = target / 40;
                        const timer = setInterval(() => {
                            counter += increment;
                            if (counter >= target) {
                                counter = target;
                                clearInterval(timer);
                            }
                            
                            if (originalText.includes('%')) {
                                value.textContent = '>' + Math.floor(counter) + '%';
                            } else if (originalText.includes('↑')) {
                                value.textContent = Math.floor(counter) + '↑';
                            } else if (originalText.includes('/1k')) {
                                value.textContent = Math.floor(counter) + ' cases/1k';
                            } else {
                                value.textContent = Math.floor(counter);
                            }
                        }, 30);
                    }, 1000);
                }
            });
            
            // Add Mouse Following Effect
            const cursor = document.createElement('div');
            cursor.className = 'cursor';
            document.body.appendChild(cursor);
            
            document.addEventListener('mousemove', function(e) {
                cursor.style.left = e.pageX + 'px';
                cursor.style.top = e.pageY + 'px';
            });
            
            // Add Cursor Styles
            const style = document.createElement('style');
            style.textContent = `
                .cursor {
                    position: fixed;
                    width: 20px;
                    height: 20px;
                    border-radius: 50%;
                    background: rgba(74, 107, 255, 0.2);
                    pointer-events: none;
                    z-index: 9999;
                    transition: transform 0.1s, width 0.3s, height 0.3s;
                    mix-blend-mode: difference;
                }
                a:hover ~ .cursor, button:hover ~ .cursor {
                    transform: scale(1.5);
                    background: rgba(255, 77, 148, 0.3);
                }
            `;
            document.head.appendChild(style);
        });
    </script>
</body>
</html>